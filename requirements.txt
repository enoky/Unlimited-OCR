# Local run: install into a .venv, then `python app.py`.
#
# transformers must stay on 4.57.1. The model ships remote code written before
# the transformers 5 rewrite; under 5.x it still loads, but the tokenizer is
# rebuilt with SentencePiece processing that eats every space and the output is
# silently garbage.
#
# huggingface-hub is pinned because 4.57.1 requires <1.0. pip will warn that
# gradio wants >=1.16 -- that bound is metadata only, gradio 6.24 runs fine
# against 0.36.
transformers==4.57.1
huggingface-hub==0.36.2

gradio==6.24.0
matplotlib>=3.7.0
einops>=0.8.0
addict>=2.4.0
easydict>=1.13
pymupdf>=1.27.0
psutil>=5.9.0
Pillow>=10.0.0

# torch/torchvision: install the CUDA build that matches your GPU, e.g. for
# Blackwell (RTX 50-series):
#   pip install torch torchvision --index-url https://download.pytorch.org/whl/cu130
torch
torchvision
